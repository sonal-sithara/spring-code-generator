spring:
  application:
    name: TempClassName
  datasource:
    url: jdbc:mysql://localhost:3306/TempClassNameDB
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080

logging:
  level:
    root: INFO
    package-des: DEBUG
